name: DIAGNOSTICO - Onde estao os arquivos?

on:
  workflow_dispatch:

jobs:
  check-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Listar tudo
        run: |
          echo "--- LISTA DE ARQUIVOS NO SEU GITHUB ---"
          ls -R
          echo "---------------------------------------"
